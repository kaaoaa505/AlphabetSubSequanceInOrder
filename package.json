{
  "name": "011_alphabetsubsequance",
  "version": "1.0.0",
  "description": "Check whether the given string is a sub sequence first to end comes in order from a to z.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node ./index.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
