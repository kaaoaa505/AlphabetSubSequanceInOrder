Check whether the given string is a sub sequence first to end comes in order from a to z.

**Example**

- For s = "effg" or s = "cdce", the output should be solve(s) = false.

- For s = "ace" or s = "bxz", the output should be solve(s) = true.